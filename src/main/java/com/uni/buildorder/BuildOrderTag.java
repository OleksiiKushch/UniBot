package com.uni.buildorder;

public enum BuildOrderTag {

    // tag (label) for starting to build SCVs non-stop (same meaning for others tags)
    BUILD_SCV_S,
    BUILD_SUPPLY_DEPOTS,

    BUILD_MARIENS,
}
